<UserControl
	x:Class="Arcusical.SongListControl"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Arcusical"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	d:DesignHeight="300"
	d:DesignWidth="400">
	<UserControl.Resources>
		<DataTemplate x:Key="SongListItemTemplate" >
			<Grid DoubleTapped="SongDoubleTapped" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
				<TextBlock Text="{Binding Title}" VerticalAlignment="Center" Margin="5" FontSize="16"/>
			</Grid>
		</DataTemplate>
	</UserControl.Resources>
	<Grid Background="#343C5C">
		<Border BorderThickness="0, 0, 2, 0" BorderBrush="#1D2133">
			<ScrollViewer>
				<ListView Height="Auto" ItemsSource="{Binding SongList.List}" ItemTemplate="{StaticResource SongListItemTemplate}">
					<ListView.ItemContainerStyle>
						<Style TargetType="ListViewItem">
							<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
							<Setter Property="VerticalContentAlignment" Value="Stretch"/>
						</Style>
					</ListView.ItemContainerStyle>
				</ListView>
			</ScrollViewer>
		</Border>
	</Grid>
</UserControl>
