<UserControl
	x:Class="Arcusical.SongPlayer"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Arcusical"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	d:DesignHeight="100"
	d:DesignWidth="600">
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="/Styles/ButtonStyles.xaml"/>
				<ResourceDictionary Source="/Converters/Converters.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>
	<Grid>
		<StackPanel Orientation="Vertical">
			<Rectangle Fill="WhiteSmoke" Stretch="Fill"/>
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
				<Button Style="{StaticResource IconStyle}"  BorderThickness="0" Width="100" Height="100" 
						HorizontalAlignment="Center" VerticalAlignment="Center"
						Click="PlayPauseButton_Clicked">
					<Grid>
						<Image Source="{Binding Path=IsPlaying, Converter={StaticResource IconConverter}}" Stretch="Fill"/>
					</Grid>
				</Button>
				<TextBlock Text="{Binding Path=Title}" FontSize="24" Foreground="DarkGray"/>
			</StackPanel>
		</StackPanel>
		<MediaElement Name="v_mediaElement"/>
	</Grid>
	
</UserControl>
