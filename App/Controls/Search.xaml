<UserControl
    x:Class="Arcusical.Search"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Arcusical"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="80"
    d:DesignWidth="607"
    Background="Transparent"
    HorizontalAlignment="Stretch">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/ColourPalette.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{StaticResource primaryBrush}">
        <TextBox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TextAlignment="Right" 
                 Text="{Binding Path=SearchTerm, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" x:Name="v_searchBox"
                 KeyDown="TextBox_KeyDown" Background="Transparent" BorderBrush="Transparent"/>
        <Image Source="/Assets/Icons/SearchIcon.png" HorizontalAlignment="Right" Margin="5,12,5,12" Opacity="1.0"/>
    </Grid>
</UserControl>
